const colors = require('tailwindcss/colors');

module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      screens: {
        xs: '425px',
      },
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
